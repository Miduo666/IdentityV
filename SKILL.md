# 内存分析工作手册

这份文档记录**方法**，不记录具体发现。所有目标相关的常量（签名、偏移、结构布局）
只存在于本地未跟踪的文件里。

适用场景：目标程序更新后，原先写死的内存偏移失效，需要重新定位。

---

## 一、先建立分层模型

把整套东西拆成三层再排查，比一上来就翻反汇编高效得多。绝大多数「更新后不工作了」
只会打中其中一层，而三层的修复成本差着数量级。

| 层 | 是什么 | 更新后的表现 | 修复成本 |
|---|---|---|---|
| **锚点层** | 运行时扫描定位根对象，不依赖固定地址 | 极少失效 | 低 |
| **链路层** | 从锚点到目标对象的几跳指针 | 偶尔整体位移 | 低 |
| **结构层** | 对象内部各字段的相对偏移 | 内容更新时可能变 | 中 |
| **展示层** | 拿到数据之后怎么筛选、怎么呈现 | 不受更新影响，但**本来就可能有逻辑缺陷** | 低 |

**结论一：基址失效通常不是问题。** 如果锚点层设计成「运行时扫描」而不是「记住上次的地址」，
那么模块重新编译、基址变化、布局整体平移，这一层都会自己找回来。看到「基址没用了」
先别慌，去确认锚点还在不在。

**结论二：先确认是哪一层坏了，再动手。** 有个反例值得记住：界面上几个数值同时显示为 0，
看起来像三处一起失效；实际上代码里它们有先后依赖，前一个没成功导致后面几行根本没执行到，
**真正坏的只有一处**。多个值同时异常时，先读代码确认数据流，再下「坏了几处」的结论。

---

## 二、分诊顺序（成本从低到高）

### 0. 用只读探针验证，不要改代码

这是最重要的一条。设备 root 之后，可以从 PC 侧直接只读目标进程内存，
几秒钟就能验证一个偏移假设。相比之下「改代码 → 编译 → 推设备 → 看日志」
一轮要好几分钟，而且每轮只能验一个猜想。

工具在 `tools/re/`，先读那里的 README（尤其是三条安全规则）。

### 1. 确认锚点还在不在

只能**在运行的进程里**确认。**不要试图用静态方法（比如在文件里搜字符串）判断** ——
运行时构造的字符串根本不在文件里，按静态结果判断会得出「引擎大改」的错误结论，
然后白白花几小时走最贵的那条路。这个坑踩过。

### 2. 锚点在 → 检查链路层

在锚点附近开一个窗口扫描，找「能走通整条链」的槽位。注意**校验必须走完全程**：
只验第一跳的话，内存里的浮点垃圾也会被当成合法指针，凭空多出一堆假候选。

### 3. 链路通 → 检查结构层

在受控条件下抓一帧全部对象的原始字节，和已知正确的旧数据逐字节对比。
按「值的分布特征」反推字段用途很有效：

- 指针列：值域集中、有固定高位特征
- 状态列：只有两三种取值，且分布符合预期比例
- 常量列：某类对象间完全一致

### 4. 以上都定位不出 → 才上反汇编

大型二进制的全量自动分析可能要几十分钟到几小时，**开始前先告知耗时预期**。
能用符号表工具解决的就别开完整的反汇编器。

---

## 三、和 AI 协作的工作流

这套流程是在 AI 辅助下跑通的。下面是实际有效的部分和踩过的坑。

### 3.1 AI 最大的价值不是「读懂汇编」

而是这三件事：

1. **批量对比**。几百个对象、每个几百字节，人眼看不过来；让 AI 写个脚本按列做集合运算，
   几秒钟列出所有「取值完全不重叠」的候选字段。这是定位新判据最快的路子。
2. **保持假设的一致性**。它会记住「这条结论是哪次采样得出的、样本多大」，
   在后面出现矛盾数据时主动翻出来对账。
3. **把一次性的探索固化成工具**。临时脚本随手就写了，但要变成下次还能用的东西，
   需要参数化、去硬编码、补文档 —— 这部分交给 AI 成本很低。

### 3.2 必须由你提供的：受控的 ground truth

这是整个流程里**最关键、且只有你能做**的一环。

与其让 AI 猜「哪些数值看起来像合理坐标」，不如你把场景固定下来并说明真实情况：
「现在场上是什么、各有几个、谁在动谁不动」。有了确切的数量和状态，就能用
**「满足某判据的对象数是否等于实际数量」** 这种硬校验一次锁定，而不是靠「像不像」。

差分实验同理：**只改变一个变量**（只移动自己、只转动视角、只触发一个交互），
前后两次快照里变化的那部分就是目标字段。

**最强的一种差分：找一对"会互换真假"的对象。**
如果目标程序里存在两个对象，在某个操作前后恰好对调状态（A 真 B 假 → A 假 B 真），
那就抓操作前后两次快照，看哪个字段在两个对象上**反向翻转**。
再挂上一组"始终真"和一组"始终假"的对照，把所有对象都在变的噪声字段排除掉。
实践中这种实验能把候选字段从几十个一次性收敛到一个，而且结论的语义是确定的，
不像横向比对那样只能得到"这个值刚好能分开"。

代价是需要你在目标里制造那个状态切换，所以**值得专门为它设计一次操作**。

### 3.3 区分三种完全不同的"字段语义"

排查时很容易把这三者混成一个"是不是真的"：

- **类型**：这个对象属于哪一类。未激活的候选对象类型也是对的，**不能当存在性用**。
- **活跃**：当前这一刻是不是生效的实例。注意「真实存在但不可见」的东西在这里可能也是"不活跃"。
- **种类/归属**：是真实的实体，还是纯装饰/占位。

单独任何一个都会漏或错，通常要组合两个。而且要接受一个现实：**有些区分在内存里根本不存在**，
它纯粹是业务语义上的差别（两个对象状态完全一致，只是设计上一个该显示一个不该）。
遇到这种情况，**类名白名单/黑名单就是正确工具，不是技术债**，不要硬找字段。

### 3.4 判据必须跨"观察位置"验证，不只是跨时间

这是本项目上代价最大的一次返工，单独拎出来讲。

很多字段的含义不是「这个对象是什么」，而是**「这个对象相对于当前观察者是什么」**——
典型的就是可见性/剔除标记。这类字段在一种观察位置下看起来完美，换一个位置就全变。

实际发生的事：某个标记在一种观察身份下，目标类别 12 个对象全部命中，
校验数量也完全吻合，于是当成「是否真实存在」用了。换一种观察身份后，同样 12 个对象
只剩 1 个命中，表现为「只能看到身边的那一个」。那个字段的真实含义是
**「当前对本机可见」**，而不是「存在」。

防范：

- **列一张"观察位置"清单**（不同阵营、不同视角、远近），新判据在每种位置下各验一次。
  数量校验只在一种位置下成立，不构成证据。
- 警惕那些**数值恰好全中**的字段。全中既可能是找对了，也可能是"当前这个观察位置下
  刚好全都可见"。
- 优先选**与观察者无关**的字段：跟对象自身生命周期绑定的，比跟渲染/剔除绑定的可靠。
  判断方法就是换位置再测一次。

### 3.5 AI 容易犯的错，以及怎么防

这一节是实打实的教训，每一条都真实发生过：

**① 单次采样就下结论。**
「某字段恰好等于实际数量」很可能是巧合。**要求多次采样、跨场景验证**，
尤其是跨一整轮流程（状态会变）。

**② 混淆「类型」和「存在性」。**
一个字段看起来能完美筛出目标，实际上它只是「类型标记」——
**未激活的候选对象在类型上当然也是同一类**。判定「本局真实存在」必须找
语义上属于「实例化」的字段（典型是一个实例化之后才非空的指针），
而不是挑一个「刚好能把当前样本分开」的值。

选判据时的排序：**语义正确 > 当前样本恰好能分开**。状态位迟早会变。

**③ 看到数字对不上就急着推翻已有结论。**
真实案例：某个标记字段数量比预期多了 3 个，AI 立刻宣布「之前的结论是巧合」。
实际上那 3 个是**处于另一种状态的同类目标**（已被淘汰但仍在场上），
结论本来是对的。**数字对不上时，先把多出来的那几个逐个查清楚是什么，再谈推翻。**

**④ 凭印象断言而不测量。**
遇到过两次「我记得需要加容差」「我记得那里已经处理了」这类判断 ——
一个测量下来是对的，一个是错的。**涉及具体数值行为的，直接读内存验证，几十秒的事。**

**⑤ 改错地方。**
如果工作目录下有多个相似的副本，明确告诉 AI 哪个是当前在维护的，
否则它可能改完、编译完，你跑的却是另一个。

**⑥ 对一个静止的量做差分，得出"这里什么都没有"。**
真实案例：要找一个倒计时字段，对几百个属性连续采样十几秒，结论是"零变化，不在这里"。
后来发现那个字段**只在倒计时进行中才非零**，当时根本没有倒计时在走。
差分之前**先制造一个已知的变化**（触发一次那个动作），再看谁跟着动。
拿静止状态做差分只会得到一个很难自己发现的假阴性。

**⑦ 从几个样本里归纳"规律"，而不去找目标自己的配置。**
真实案例：三个样本里某类 id 都满足"末尾几位固定"，于是按这个规律写了匹配。
后来在目标进程里找到了它自己的配置表，发现这个规律只对一部分成立，
另一部分完全不符合。**样本全中不等于规律成立**，样本可能恰好都落在规律成立的那一块。
如果目标自带权威的配置数据，**优先把它导出来**，而不是反推。

**⑧ 旧判据失效时，只顾着修旧判据。**
两个对象外观/类名完全一样、只能靠某个状态字段区分时，那个字段可能随更新改变语义。
与其猜它现在变成了什么，不如**在受控状态下把两个对象整段字节对比一次**，
找一个能干净分开两者的新字段。选的时候注意：有的字段整个字节会跳变，
但其中某一位是稳定的，**判据只看稳定的那一位**，不要比较整个字节。

### 3.6 怎么验收 AI 的结论

要求它把结论拆成三部分：**「测量到的原始数据 / 由此得出的推断 / 还没验证的假设」**。
只有第一部分可以直接信。养成这个习惯之后，前面那些错误在提交前基本都能拦住。

---

## 四、写修复代码的原则

### 4.1 自愈优于硬编码

发现某个写死的偏移从 A 变成了 B，**先看它周围是不是一组等间隔的同类项**。
如果是，说明整体位移过，写死新值下次照样失效。

正确做法是改成「在窗口内扫描，用**能否走通后续链路**做校验」——
这一类失效以后就自己吸收了。

### 4.2 有数学关系可用时，用它当校验器

某些量既存在于内存里，也能从其他数据推算出来。推算精度可能不足以直接用，
但**足够用来在候选里挑出正确的那个**。这种「自带校验器」的定位方式比任何硬编码都稳。

### 4.3 指针合法性要卡上下界

只判下界会让浮点垃圾被当成合法指针。同时卡上界，并且尽量用「整条链都走得通」
而不是单跳来校验。

### 4.4 展示层的缺陷和内存偏移无关，别混为一谈

目标程序往往会把**所有候选**都实例化，本轮只激活其中一部分。
「按类名匹配就显示」必然多显示。这类问题的根因在展示逻辑，不在偏移。

两个相关经验：

- **有距离/范围限制的分支会掩盖这个问题**，只在走近时才暴露。没有限制的分支先出问题，
  但不代表其他分支是好的。
- **调试用的绘制往往是独立分支**，不走正常的分类过滤。给某个类别加了过滤之后，
  调试视图里照样会看到被过滤掉的东西 —— 别以为是过滤没生效。

### 4.5 加过滤之前，先看已有的

在加新的判断之前先通读一遍现有的过滤链。常见的边界情况（无效坐标、超出范围、
背向剔除）可能已经处理过了，重复添加只会让逻辑更难维护。

### 4.6 "读不到"和"没有"必须是两种返回值

跨进程读取会间歇失败（页被换出、对象正在被替换）。如果读失败和"确实为空/确实没有"
返回同一个结果，上层就只能二选一地猜，而且猜错会直接显示成错误的结论。
**函数签名层面就把三种状态分开**：读失败 / 读到了且为空 / 读到了且有内容。

### 4.7 会间歇读失败的静态数据：增量累积 + 二次确认

对于"在一个周期内不会变、但单次读取经常只读到一部分"的数据（比如一个列表）：

- **每轮读到几条就合并几条**，不要求单次读全。只要多读几轮，缓存会自己补全。
- **单条数据要在两个不同轮次读到相同值才确认**。进缓存的东西会一直留着，
  一次偶发的垃圾读取不能直接变成永久结论。
- **用容器自身的长度判断完整**：长度本身通常读得稳，已确认条数等于长度才算完整。
  长度变了、或已确认的比长度还多，说明缓存不可信，整份重来。
- **只有"完整"时才能断言"没有某一项"**。不完整时只能说"已确认有哪些"。
- 如果有目标自带的取值范围（比如某一项只允许哪几个值），拿它做逐条校验。

### 4.8 验收条件只判断"读全了没有"，不要掺进"通常是什么样"

真实案例：为了防止缓存残缺数据，加了一条"必须包含几项常见内容才接受"的验收。
它确实挡住了残缺数据，但也把**正常但少见**的情况永远挡在了外面，
而且这类对象从此既不显示也不报错，很难被发现。
验收只应该回答"这次读到的是不是完整、可信"，**业务上的预期不属于验收条件**。

### 4.9 读取周期比显示周期慢时：推算显示，状态切换以实测为准

对于按已知速率变化的量（比如一个匀速递减的倒计时），读取周期再短也会一跳一跳的。
更好的做法是记下"读到的值 + 读到的时刻"，显示时按经过的时间推算。注意两点：

- 速率如果有对应的字段（倍率之类），推算时乘上它，不要假设恒定。
- **离散的状态切换（比如"已就绪"）只以真实读到的值为准**，不要靠推算到边界来判定。
  推算可能略快，目标也可能在两次读取之间直接改写这个值；
  下一次读取会把推算纠正回来，误差不超过一个读取周期。

---

## 五、目录

```
jni/            源码
tools/re/       PC 侧只读分析工具（见其 README）
  config.py     本地配置，含目标相关常量，已 gitignore
SKILL.md        本文档
```

## 六、提醒

本项目的免责声明见 `README.md`。这份文档描述的是通用的内存分析方法，
使用者需自行确保其行为符合所在地区法律法规及相关服务条款。
