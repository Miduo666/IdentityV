# 使用说明 (Usage Guide)

## 📋 系统要求

| 要求 | 说明 |
|------|------|
| 📱 设备 | Android 设备 |
| 🎮 游戏 | 第五人格 (Identity V) 所有版本 |
| 🔓 权限 | **Root 权限** (必需) |

---

## 🚀 使用步骤

### 1. 放置文件

将程序文件放置到 `/data` 目录下

### 2. 选择驱动

从 `hook_ioctl有缓兼容无敌` 压缩包中选择适配你设备的驱动

### 3. 设置权限

给予 777 权限(驱动和qq.sh均需要)

### 4. 刷入驱动

**先刷入驱动**，确保驱动加载成功

### 5. 启动游戏

打开第五人格游戏

> ⚠️ **注意**: 必须先打开游戏再执行程序，否则会空载！

### 6. 执行程序

勾选使用ROOT权限执行qq.sh

### 7. 唤出悬浮窗

使用 **音量键** 唤出/隐藏悬浮窗

| 按键 | 功能 |
|------|------|
| 音量+ | 显示悬浮窗 |
| 音量- | 隐藏悬浮窗 |

---

## ⚠️ 注意事项

- ✅ 确保设备已获取 Root 权限
- ✅ 确保驱动与设备匹配
- ✅ 必须先启动游戏再运行程序
- ❌ 游戏未启动时运行会导致空载

---

## 📁 目录结构

### 工程目录（本压缩包）

```
IdentityV/
├── 使用说明.md                    本文件
├── README.md                      项目说明 + 免责声明
├── SKILL.md                       内存分析工作手册（方法论，不含目标常量）
├── 1.bat                          一键编译（随机化模块名/优化等级/熵后调用 ndk-build）
├── aide_ndk.sh                    手机端 AIDE 编译入口
├── <8位随机字母>.sh               编译产物（每次编译名字不同）
├── hook_ioctl有缓兼容无敌.zip     内核驱动（按设备选择）
├── 007开源骨骼进度绘制优化.zip    注入子系统所在的旧工程快照
│
├── jni/
│   ├── Android.mk / Application.mk
│   ├── tools/gen_build_vars.ps1   编译随机化脚本（1.bat 调用）
│   ├── include/                   第三方与公共头文件
│   │   ├── Android_draw/          EmbeddedHookSo.h（内嵌 hook so）、SoHookIntegration.h
│   │   ├── Android_Graphics/      OpenGL / Vulkan 封装
│   │   ├── Android_my_imgui/      ImGui Android 适配
│   │   ├── Android_touch/         触摸
│   │   ├── ImGui/                 ImGui 本体、后端、字体
│   │   ├── My_Utils/              stb_image 等
│   │   └── native_surface/        悬浮窗 Surface 创建
│   └── src/
│       ├── main.cpp
│       ├── build_entropy.h        编译时自动生成的随机字节
│       ├── Android_draw/
│       │   ├── draw_Gui.cpp       主绘制逻辑
│       │   ├── Name.h             类名 → 中文名映射
│       │   ├── kerneldriver-qxqd.hpp  驱动读内存接口
│       │   ├── PyProgress.h       密码机 / 大门进度（Python 层）
│       │   ├── PyGenius.h         天赋、辅助特质与冷却（Python 层）
│       │   ├── PySelf.h           自身识别、废弃模型过滤（Python 层）
│       │   ├── SoHookIntegration.cpp  注入子系统（本工程中未启用）
│       │   └── AndroidImgui.cpp / DrawTool.h / timer.h
│       ├── Android_Graphics/ Android_my_imgui/ Android_touch/ ImGui/ My_Utils/
│       └── res/                   cJSON 等
│
├── tools/re/                      PC 端只读内存分析工具（先读其中 README.md）
│   ├── memread.py / probe.py / diff_objects.py
│   ├── config.example.py          配置模板，复制为 config.py 后填本机路径
│   └── local-payload-audit/       载荷字段审计笔记
│
└── injection-research/            注入子系统复习与操作目录
    ├── SKILL.md                   操作手册（先读）
    ├── injection.md               完整注入笔记
    ├── embed_payload.py           查看 / 生成 EmbeddedHookSo.h
    └── probe2.py                  进程内只读探针
```

### 设备端（部署后）

```
/data/
├── <8位随机字母>.sh     编译出的可执行文件，权限 755/777
└── <驱动文件>           从 hook_ioctl有缓兼容无敌.zip 中选出的适配驱动
```

---

## 🔧 常见问题

### Q: 程序无响应？
A: 检查是否先启动了游戏，未启动会空载。

### Q: 悬浮窗不显示？
A: 尝试按音量+键唤出。

### Q: 权限不足？
A: 确保已执行 `chmod 777` 并具有 Root 权限。

---

**使用本软件即表示您已阅读并同意 [免责声明](README.md)**
